let str = "microsoft";
console.log(str[2]);
console.log(str[9]);

//first index is start with zero
//*lst index - lenght - 1

//can we determine lenght of the string -> str.lenght
console.log(str.length);

for(let idx = 0; idx <= str.length -10; idx++) {
    console.log(idx, str[idx]);
}